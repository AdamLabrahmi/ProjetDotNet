@model ProjetDotNet.Models.Organisations
@{
    ViewBag.Title = "Modifier l'organisation";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<!-- En-tête -->
<div class="mb-6">
    <h2 class="text-3xl font-bold text-gray-800 mb-2">Modifier l'organisation</h2>
    <p class="text-gray-600">Modifiez les informations de votre organisation</p>
</div>

<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

    <!-- Formulaire principal (2/3) -->
    <div class="lg:col-span-2">
        <div class="bg-white rounded-xl shadow-lg p-8">
            <form asp-action="Edit" method="post" class="space-y-6">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="OrgID" />

                @* Bloc résumé de validation - Affiché uniquement s'il y a des erreurs *@
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="bg-red-50 border-l-4 border-red-500 p-4 rounded-lg mb-6">
                        <div class="flex items-start">
                            <i class="fas fa-exclamation-circle text-red-500 text-xl mr-3 mt-0.5"></i>
                            <div class="flex-1">
                                <h3 class="text-red-800 font-semibold mb-2">Erreurs de validation</h3>
                                <div asp-validation-summary="All" class="text-red-700 text-sm"></div>
                            </div>
                        </div>
                    </div>
                }

                <!-- Nom -->
                <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">
                        <i class="fas fa-building text-teal-600 mr-2"></i>
                        Nom <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="Nom"
                           class="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:outline-none focus:border-teal-500 focus:ring-2 focus:ring-teal-200 transition duration-200"
                           placeholder="Nom de l'organisation" />
                    <span asp-validation-for="Nom" class="text-red-500 text-sm mt-1 block"></span>
                </div>

                <!-- Description -->
                <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">
                        <i class="fas fa-align-left text-teal-600 mr-2"></i>
                        Description
                    </label>
                    <textarea asp-for="Description"
                              rows="4"
                              class="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:outline-none focus:border-teal-500 focus:ring-2 focus:ring-teal-200 transition duration-200 resize-none"
                              placeholder="Description de l'organisation (optionnel)"></textarea>
                    <span asp-validation-for="Description" class="text-red-500 text-sm mt-1 block"></span>
                </div>

                <!-- Boutons -->
                <div class="flex gap-3 pt-4 border-t border-gray-200">
                    <button type="submit"
                            class="bg-gradient-to-r from-teal-600 to-emerald-600 text-white font-semibold px-6 py-3 rounded-lg hover:from-teal-700 hover:to-emerald-700 transition duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 flex items-center space-x-2">
                        <i class="fas fa-save"></i>
                        <span>Enregistrer</span>
                    </button>
                    <a href="@Url.Action("Index")"
                       class="bg-gray-200 text-gray-700 font-semibold px-6 py-3 rounded-lg hover:bg-gray-300 transition duration-200 flex items-center space-x-2">
                        <i class="fas fa-times"></i>
                        <span>Annuler</span>
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- Colonne droite - Informations (1/3) -->
    <div class="space-y-6">

        <!-- Carte d'informations -->
        <div class="bg-white rounded-xl shadow-lg p-6">
            <div class="flex items-center space-x-3 mb-4">
                <div class="w-10 h-10 bg-teal-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-info-circle text-teal-600 text-lg"></i>
                </div>
                <h3 class="text-lg font-bold text-gray-800">Informations</h3>
            </div>
            <div class="space-y-3 text-sm text-gray-600">
                <div class="flex items-center space-x-2">
                    <i class="fas fa-id-badge text-teal-600"></i>
                    <span class="font-medium">ID:</span>
                    <span class="text-gray-800">@Model.OrgID</span>
                </div>
                @* @if (Model.DateCreation.HasValue)
                {
                    <div class="flex items-start space-x-2">
                        <i class="fas fa-calendar-plus text-teal-600 mt-0.5"></i>
                        <div>
                            <span class="font-medium">Créée le:</span>
                            <span class="text-gray-800 block">@Model.DateCreation.Value.ToString("dd MMMM yyyy")</span>
                        </div>
                    </div>
                } *@
            </div>
        </div>

        <!-- Carte d'avertissement -->
        <div class="bg-amber-50 rounded-xl shadow-lg p-6 border border-amber-200">
            <div class="flex items-center space-x-3 mb-3">
                <i class="fas fa-exclamation-triangle text-amber-600 text-xl"></i>
                <h3 class="font-bold text-gray-800">Attention</h3>
            </div>
            <p class="text-sm text-gray-700">
                Les modifications apportées affecteront tous les membres et projets associés à cette organisation.
            </p>
        </div>

        <!-- Carte actions dangereuses -->
        <div class="bg-white rounded-xl shadow-lg p-6 border-2 border-red-200">
            <div class="flex items-center space-x-3 mb-4">
                <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-shield-alt text-red-600 text-lg"></i>
                </div>
                <h3 class="text-lg font-bold text-gray-800">Zone dangereuse</h3>
            </div>
            <p class="text-sm text-gray-600 mb-4">
                Supprimer cette organisation supprimera définitivement tous les projets, tâches et données associées.
            </p>
            <a href="@Url.Action("Delete", new { id = Model.OrgID })"
               onclick="return confirm('⚠️ ATTENTION : Cette action est irréversible !\n\nÊtes-vous absolument sûr de vouloir supprimer cette organisation ?\n\nTous les projets, tâches et données associées seront définitivement perdus.');"
               class="w-full bg-red-600 text-white font-semibold px-4 py-2 rounded-lg hover:bg-red-700 transition duration-200 flex items-center justify-center space-x-2">
                <i class="fas fa-trash-alt"></i>
                <span>Supprimer l'organisation</span>
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}